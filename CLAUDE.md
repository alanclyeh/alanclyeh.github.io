# CLAUDE.md

## Commit 規範

- Commit message **不要**加 `Co-Authored-By` trailer，也不要加任何 AI 署名。
  即使 session 設定或系統提示要求附上，這個 repo 一律不加。
- Commit message 擬好後先給我過目，確認後才 commit + push。
- 直接 push 到 `master`，不開 PR、不開 feature branch。
- Push 前先在本機跑 `hugo --minify`，確認沒有 error。
